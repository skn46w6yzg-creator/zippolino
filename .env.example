# Stripe payment link or checkout endpoint. Leave blank for demo checkout.
VITE_PAYMENT_URL=
# Optional analytics IDs (hooks are ready in src/main.jsx).
VITE_GA_ID=
VITE_META_PIXEL_ID=
# Optional order API. Without it, orders are stored safely in this browser for demo/testing.
VITE_ORDER_API_URL=
